# 安全说明

知时是单用户本地桌面应用。后端默认监听回环地址并校验 Host 和 Origin，不提供公网账号认证或多租户隔离。请勿将端口通过反向代理、隧道或端口转发开放给不受信任的设备。

## 数据与外部服务

- 模型 API Key 通过系统凭据库保存，数据库仅保存引用。其他配置、会话、附件和业务记录仍需依赖操作系统账户及磁盘权限保护。
- 向模型、联网服务或 MCP 发送请求时，相关输入可能离开本机。第三方服务的数据处理与保留规则由相应服务方决定。
- 本地 MCP stdio 服务会以当前用户权限运行程序。仅配置可信来源，审查启动命令和权限，不向不熟悉的服务授予永久操作权限。
- 页面内容、附件和模型输出都不能视为可信指令。应用设有工具权限与审批检查，涉及重要数据的操作仍应核对内容。

## 备份与报告问题

备份前从托盘彻底退出应用，再复制数据目录。备份可能包含私人记录和附件，应妥善保存；系统凭据库中的模型密钥需另行管理。

报告问题时仅提供去除敏感信息的复现步骤和最小样本。不要在公开 Issue 中附完整数据库、日志、备份、密钥或私人对话。若怀疑密钥泄露，应先到对应服务平台撤销并更换密钥；删除仓库当前文件不会清除 Git 历史或其他人的副本。
