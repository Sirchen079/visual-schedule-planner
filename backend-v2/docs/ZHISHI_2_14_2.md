# 知时 2.14.2

产品名称统一为 **知时**，应用、安装包、快捷方式均去掉展示用的 v2 后缀。安装包：`zhishi-Setup-2.14.2.exe`。桌面包、Python 包版本与 `/health` 版本均为 2.14.2。

主界面右上角常驻 **GitHub · Star**，设置页顶部显示 **喜欢知时？点个 Star**，均跳转到 [项目仓库](https://github.com/Sirchen079/visual-schedule-planner)。外链在系统浏览器打开，使用 noreferrer / no-referrer，应用不会自动替用户 Star，也不会读取 GitHub 凭据。

本版不兼容老知时（1.x）数据格式，不自动扫描、读取或迁移老版数据库。2.x 独立数据路径保持稳定，产品改名不会主动搬运数据。

## 验证

- 版本、健康接口、打包与配置测试 8 项通过；桌面壳现有 9 项通过。
- 排除个人课表附件后的公开源码完整后端测试：762 passed、5 skipped、1 条既有依赖弃用警告。3 个跳过来自未发布的真实课表附件，另有2个既有跳过。
- 前端 TypeScript 和 Vite 构建通过。
- 冻结程序原生验收通过：产品标题、常驻/设置页外链的精确 URL、页面不会被外链替换、900px 布局；测试使用伪造旧格式数据库，启动后旧文件哈希不变且新数据库不包含旧任务。
- `scripts/verify_project_link.py`、`.cjs`；报告 `release/desktop-2.14.2/qa/brand.json` 与 `brand-ui.json`。安装包与前后端、桌面壳资源逐文件核对结果及 SHA256 见 `verification.json`。未运行真实安装向导，未执行 GitHub Star。

## 公开源码

公开源码不包含本地数据库、备份、运行日志、真实个人附件、模型密钥、依赖目录和内部工作记录。通过显式源码清单准备提交；凭据扫描的命中均核对为测试占位值。提交身份使用项目所有者指定的 GitHub noreply 地址。

旧版仓库文件保留作历史参考，新版默认启动入口与 README 指向当前代码。无需购买服务器，本版本也没有新增自动云日历同步。
