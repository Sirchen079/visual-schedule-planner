# 知时 2.21.1

修复 Windows 更新时的退出等待流程，并改为自动安装后重新启动。

- 多个退出请求等待同一次关闭完成，重复退出事件也会等待后端。
- 后端超时需要强制结束时，等待进程实际退出后再安装；关闭失败会停止安装并显示原因。
- 点击更新后保存草稿，自动安装并启动新版，无需继续操作安装向导。
- 增加更新准备、安装请求和更新失败的诊断记录，便于定位失败阶段。

旧版本仍使用旧的更新流程。若更新后版本未变化，请从托盘退出知时，再运行 `zhishi-Setup-2.21.1.exe` 覆盖安装到原目录。
