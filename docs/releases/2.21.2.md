# 知时 2.21.2

修复 Windows 更新后仍启动旧版本的问题。

- 更新时检查桌面和开始菜单快捷方式，修正指向旧安装副本的路径。
- 保留快捷方式里的自定义启动参数、图标、热键和窗口状态；已删除的快捷方式不会重新创建。
- 安装完成后核验启动目标；快捷方式不可用时直接启动刚安装的新版本。

包含 2.21.1 的退出与自动重启修复。用户数据保留。

如果旧版更新后版本仍未变化，请从托盘退出知时，运行本页的安装包，选择原安装目录覆盖安装。
