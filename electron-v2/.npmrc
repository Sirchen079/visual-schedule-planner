# Electron 二进制与 electron-builder 产物下载镜像（国内网络）
# 说明：@electron/get 与 electron-builder 只认 .npmrc / 环境变量，不读 package.json 自定义字段，
#       故镜像配置落在本文件；等价环境变量 ELECTRON_MIRROR 亦可。
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
